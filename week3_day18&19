var browser="Safari";

if (browser=="Edge"){
  document.write("you have got the edge");
}

else if (browser=="Chrome" || browser=="Firefox" || browser=="Safari" || browser=="Opera"){
  document.write("ok we support those browser too");
}

else {
  document.write("we hope that this page it looks good");
}
